// Конфигурация для клиентской части
export const config = {
  admin: {
    defaultUsername: "KattyFit",
    // В production эти значения должны быть установлены через переменные окружения
    // NEXT_PUBLIC_ADMIN_USERNAME и NEXT_PUBLIC_ADMIN_PASSWORD
  }
} as const
